# Watch [NodeJS]
Watch file changes

### Install
```
npm install kc-watch
```

### Use
```js
var watch = require('kc-watch');
watch('.', function(d, f, e){
    if (
    !d.match(/^\..*/g) &&
    !f.match(/^\..*/g)
    ) {
        console.log(e, d+'/'+f);
    }
});
```
